# P5.1 Suggestions IA sur Boards

## Objectif

Ajouter des suggestions IA aux boards visuels : proposer de nouveaux noeuds, résumer, trouver des connexions, générer des images.

## Prérequis

- [x] P2.2 - Boards visuels basiques

## Fichiers à Créer

- `web-ui/src/components/boards/AISuggestions.tsx`
- `web-ui/server/services/board_ai_service.py`
- `web-ui/server/routes/board_ai.py`

## Spécifications

### Actions IA

| Action | Trigger | Résultat |
|--------|---------|----------|
| Suggest nodes | Auto / bouton | Propose nouveaux éléments basés sur le contenu |
| Summarize | Bouton | Crée un noeud "résumé" du board |
| Find connections | Bouton | Suggère des liens entre noeuds |
| Expand | Click sur noeud | Génère sous-noeuds |
| Generate image | Click sur noeud texte | Crée un visuel via Replicate/DALL-E |

### API

```python
# POST /api/boards/{id}/ai/suggest
# Génère des suggestions de noeuds

# POST /api/boards/{id}/ai/summarize
# Résume le board en un noeud

# POST /api/boards/{id}/ai/connections
# Trouve des connexions possibles

# POST /api/boards/{id}/nodes/{node_id}/expand
# Génère des sous-noeuds

# POST /api/boards/{id}/nodes/{node_id}/generate-image
# Génère une image depuis le texte
```

### UI Suggestions

```
┌─────────────────────────────────────────────────────────────┐
│  💡 Suggestions IA                                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Basé sur ton mood board "Temple Style":                   │
│                                                             │
│  [+ Ancient stone textures]                                │
│  [+ Bioluminescent plants]                                 │
│  [+ Mystical fog effects]                                  │
│                                                             │
│  [🔗 Trouver connexions]  [📝 Résumer]                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## Critères d'Acceptation

- [ ] Suggestions de noeuds générées par LLM
- [ ] Ajout de noeud depuis suggestion en un clic
- [ ] Résumé du board fonctionnel
- [ ] Détection de connexions possibles
- [ ] Expansion de noeud fonctionnelle
- [ ] Génération d'images (si provider configuré)
- [ ] Tests passent
