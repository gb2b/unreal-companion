# Unreal Companion - Project Configuration
# This file is auto-generated and can be customized per project

version: "1.0"
project_name: "{{project_name}}"
created_at: "{{date}}"

# User preferences
user_name: ""
communication_language: "English"
document_output_language: "English"

# Paths (relative to .unreal-companion/)
output_folder: "output"
agents_folder: "agents"
workflows_folder: "workflows"
sessions_folder: "sessions"

# Default agent for chat
default_agent: "game-designer"

# Unreal Engine connection
unreal:
  host: "127.0.0.1"
  port: 55557
  auto_connect: true

# LLM preferences (uses global settings if not specified)
llm:
  # Override global provider for this project
  # provider: "anthropic"
  # model: "claude-sonnet-4-20250514"
