# Unreal Companion - Project Agents Configuration
# Agents listed here are available in the Studio UI

# Default agents (from system templates)
default:
  - game-designer
  - game-architect
  - game-dev
  - 3d-artist
  - level-designer

# Custom agents created for this project
# Place YAML files in ./custom/ folder
custom: []

# Overrides for default agents
# Place YAML files in ./overrides/ folder
# Example: ./overrides/game-designer.yaml to customize the designer
overrides: []
